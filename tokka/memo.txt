youtube.com/watch?v=0BfkcaHXen8

https://ikasenmo-answer.com/windows10-disk-100/

Option Explicit

'閉じるボタン
Private Sub CommandButton1_Click()
    Unload UserForm1
End Sub

*~*~*~*~*~*~*~*~*~*~*~*~*

Private Sub Nobox_Change()
    Dim his_id As Long
    his_id = Nobox.Value
    UserForm1.TextBox1.Text = Cells(his_id, 2).Value
End Sub

Private Sub SpinButton1_SpinUp()
    Dim his_id As Long
    Nobox.Value = Nobox.Value + 1
    his_id = Nobox.Value
    UserForm1.TextBox1.Text = Cells(his_id, 2).Value

End Sub

Private Sub SpinButton1_SpinDown()
    Nobox.Value = Nobox.Value - 1
    his_id = Nobox.Value
    UserForm1.TextBox1.Text = Cells(his_id, 2).Value
End Sub

Private Sub TextBox1_Change()

End Sub

Private Sub UserForm_Initialize()
    Dim MaxRow As Long
    MaxRow = Cells(Rows.Count, 1).End(xlUp).Row
    UserForm1.Nobox.Value = MaxRow + 1
    
    Dim txt_value As Variant
    UserForm1.TextBox1.Text = Cells(MaxRow, 2).Value
End Sub


